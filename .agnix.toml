severity = "Warning"
exclude = [
    "node_modules/**",
    ".git/**",
    "target/**",
    ".personal/**",       # gitignored local-only scratch
    "RULES.md",           # XML-balance false positives on type signatures (`list<string>`) in tables
]
target = "Generic"
tools = []
max_files_to_validate = 10000

[rules]
skills = true
hooks = true
agents = true
memory = true
output_styles = true
plugins = true
xml = true
mcp = true
imports = true
cross_platform = true
agents_md = true
copilot = true
cursor = true
cline = true
opencode = true
gemini_md = true
codex = true
roo_code = true
windsurf = false      # never targeted (sole contributor uses amp/cursor)
kiro_steering = true
kiro_agents = true
amp_checks = true
prompt_engineering = true
generic_instructions = true
frontmatter_validation = true
xml_balance = true
import_references = true
disabled_rules = ["CDX-AG-005", "AGM-003", "PE-001"]  # CDX-AG-005: i18n broken in agnix 0.20.0 (re-enable when fixed). AGM-003: Windsurf 12k char cap — never targeted. PE-001: "lost in the middle" keyword-position heuristic — crude for a solo-maintained instruction file.
disabled_validators = []

[tool_versions]

[spec_revisions]

[files]
include_as_memory = []
include_as_generic = []
exclude = []
