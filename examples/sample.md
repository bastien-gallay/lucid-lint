# Example input

This short paragraph is clean. It has two brief sentences. Neither triggers a rule.

## A paragraph with a long sentence

This is a rather long sentence that keeps adding more and more words until it definitely exceeds the public profile threshold by a comfortable margin which is the whole point of this demonstration example here.

Normal paragraph follows.

```rust
// Code blocks are excluded from linting.
let sentence = "This code snippet is not analyzed because it's in a fenced block.";
```
